from tasteMath import thingy
from customer_class import get_customer_attributes
#-------
imTrying = {
    "lemons": 3,
    "sugar": 0,
    "ice": 0
}
#-------
print(thingy(imTrying))
sweetness = 100
# I need to take lemon + sugar to make "sweetness".
if "lemons" += 1:
    sweetness -= 15
if "sugar" += 1:
    sweetness += 5